services:
  - type: web
    name: lead-qualifier-api
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL=postgresql://leadqualifier:lW1TPjbXXkNoX67UCAD81mjI23HMObZB@dpg-d6cvi4f5r7bs73amrsng-a/leads_db_12to
        fromDatabase:
          name: lead-qualifier-db
          property: connectionString
      - key: PYTHON_VERSION
        value: 3.11.9